//const { driverSetup } = require('../utils/Driver')
const { driverSetup } = require('../utils/DriverHeadless')
const { cssSelectorsPractise } = require('../scripts/CSS_Selectors_Practise')

module.exports = {
    driverSetup,
    cssSelectorsPractise
}